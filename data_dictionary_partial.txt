data dictionary partial

players
- player_id INTEGER primary key autoincrement unique identifier for a player
- name TEXT not null full name for display and search
- email TEXT unique optional contact email validate format when entered
- joined_date TEXT iso8601 date when account created

card_sets
- set_id INTEGER primary key autoincrement unique identifier for a card set
- name TEXT not null descriptive set name
- code TEXT unique short set code used for quick lookup
- release_date TEXT iso8601 release date for the set

cards
- card_id INTEGER primary key autoincrement unique card identifier
- name TEXT not null card name
- set_id INTEGER not null references card_sets(set_id) set membership
- rarity TEXT controlled vocabulary eg common uncommon rare ultra
- card_type TEXT e.g. creature spell trainer
- mana_cost TEXT textual representation or null for non applicable

(deferred) continue for decks deck_cards tournaments tournament_entries
